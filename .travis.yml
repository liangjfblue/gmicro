language:
  - go

sudo: true

go:
  - "1.13.x"

env:
  - GO111MODULE=on

before_install:
  - chmod +x ./scripts/build.sh all
  - chmod +x ./scripts/dockerfile.sh all

script:
  - ./scripts/build.sh
  - ./scripts/dockerfile.sh
  - cd deployments && sudo docker-compose up